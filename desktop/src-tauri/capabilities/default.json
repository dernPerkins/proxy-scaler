{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "proxy-scaler-serve",
          "cmd": "$RESOURCE/proxy-scaler-serve/proxy-scaler-serve",
          "sidecar": false,
          "args": true
        }
      ]
    }
  ]
}
